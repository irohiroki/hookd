server:
  host: "0.0.0.0"
  port: 9000

log:
  file: /home/rocky/webhook/webhook.log
  level: INFO
  max_bytes: 10485760
  backup_count: 5

routes_dir: /home/rocky/webhook/routes.d

routes: []
# Example route:
#
#  - path: /deploy/my-app
#    secret: ""                      # shared passphrase for HMAC-SHA256 verification; omit to skip
#    script: /home/rocky/scripts/deploy.sh
#    async: true                     # true = fire-and-forget (always responds 202)
#    timeout: 60                     # seconds; ignored when async: true
#    match:                          # all conditions must match; omit to match any payload
#      ref: "refs/heads/main"
#    env:                            # extra env vars forwarded to the script
#      APP_ENV: production
